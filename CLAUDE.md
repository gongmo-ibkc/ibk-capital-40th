# IBK캐피탈 창립 40주년 기념 사이트

## 프로젝트 개요
- 배포 주소: https://gongmo-ibkc.github.io/ibk-capital-40th/
- 저장소: https://github.com/gongmo-ibkc/ibk-capital-40th (GitHub Pages, main 브랜치 루트)
- 단일 파일 사이트: `index.html` 하나에 CSS/JS/오디오(base64 2.8MB)까지 전부 내장
- 콘셉트: "혁신의 파도" — 40주년 헌정곡과 파도 모티프를 사이트 전체의 디자인 언어로 사용
- 이 프로젝트는 Claude(Cowork)에서 개발되어 Claude Code로 이관됨

## 파일 구조 (이 키트)
- `src.html` — **편집용 소스** (최신). 오디오 자리에 `__SAGA_SRC__`, 로고 자리에 `__LOGO_SRC__` 플레이스홀더. 모든 수정은 이 파일에서.
- `saga.mp3` — 40주년 헌정곡 「혁신의 파도」 (128kbps, 2:13.8)
- `logo.png` — IBK캐피탈 공식 로고 (500×112 투명 PNG, 14KB). 푸터(밝은 배경)에 원본 컬러로 사용.
- `logo-white.png` — 헤더용 리버스 버전. 심볼은 원본 컬러 유지, 글자만 흰색
  (logo.png에서 x≥128 영역의 불투명 픽셀을 흰색으로 치환해 생성. 전체 흰색 필터를 쓰면 심볼 디테일이 사라지므로 금지).
- `build.py` — src.html + saga.mp3 + logo.png + logo-white.png → index.html 생성
  (플레이스홀더 3개 치환: `__SAGA_SRC__`, `__LOGO_SRC__`, `__LOGO_W_SRC__`)
- `og-image.png` — 공유 카드 (1200x630, 저장소에 함께 배포). 2026-07-23 리뉴얼: 사이트 현행 디자인(남색 바다·
  부유 숫자·금색 포인트·파도 라인) 반영, PIL 스크립트로 생성 (맑은 고딕 볼드 사용)
- `build_gallery.py` — 사진/ 원본 → photos/g###.jpg 갤러리 썸네일 + src.html GALLERY 매니페스트 자동 갱신
  (사진 추가 시: 사진/에 `YYYYMM_설명.jpg` 넣고 이 스크립트 → build.py 순서로 실행)
- `photos/` — 갤러리 썸네일 g###.jpg 173장 + 라이트박스 확대본 f###.jpg 173장 (총 13.4MB, 저장소에 함께 배포).
  `사진/` 원본 폴더는 .gitignore 제외
- `index.html` — 빌드 결과물 (직접 편집 금지)

## 빌드 & 배포
```bash
python3 build.py          # index.html 생성
git add -A && git commit -m "..." && git push   # push → Pages 1~2분 내 자동 배포
```

## 키오스크(전시) 모드 — `?kiosk=1` (2026-08-03 신설, 회사 TV 디스플레이용)
- 용도: 사내 가로형 스마트TV 등 무인 디스플레이. URL에 `?kiosk=1`만 붙이면 켜짐 — 일반 방문자 경험에는 영향 없음.
- 동작: 18페이지 자동 투어 무한 루프. 페이지별 체류(초): 히어로 9 / 스토리 12 / 숫자 프롤로그 8 /
  차트 13 / 통계 14 / 사람의 힘 11 / 함께한 순간들 22 / 축하 한마디 16 / 비전 12 (DWELL 객체).
- 음소거 고정: 자동재생·제스처 재시도 IIFE 전체를 `if(!KIOSK)`로 건너뜀. 사가 버튼·song-tag 숨김.
- 축하 한마디: 입력 폼(.cheer-form/.cheer-status)은 숨기고 Firebase 실시간 카드만 표출 — 직원이 폰으로 남기면 TV에 실시간 반영.
- 참여 QR(2026-08-03): 폼 자리에 .cheer-qr(흰 카드 + 안내 문구) 표시 — `#cheers` 앵커 URL을 담은
  33×33 QR을 **인라인 SVG(3.8KB, 단일 path)로 내장** (python qrcode 라이브러리로 생성, EC 레벨 M).
  jsQR로 디코드 검증 완료. 일반 웹에서는 숨김(.cheer-qr{display:none} — 폼이 있으므로 불필요).
  화면 표시 크기 약 272px(zoom 1.4 포함) + 흰 패딩 13px(콰이어트 존). URL 변경 시 QR 재생성 필요.
- 수동 조작(휠/터치/포인터/키) 감지 시 투어 정지 → 45초 무입력이면 가장 가까운 페이지부터 재개.
- TV 시청 거리 확대(2026-08-03, 헤더 제거 후 1.4→1.5 상향): `.page .wrap{zoom:1.5}` + `.hero-inner{zoom:1.5}`
  — **히어로는 .wrap이 없고 .hero-inner 구조**라 별도 지정 필수(빠뜨리면 히어로만 안 커지는 버그, 실제 있었음).
  차트 페이지는 JS 템플릿이 .wrap을 주입하므로 커버됨. 헤더 제거로 kiosk .page{padding-top:48px}(기본 92px).
  히어로 줄글(p.sub)은 kiosk max-width:960px로 가로 확장(2줄 유지). 코너 브랜드(사용자 확정 배치):
  좌하단 IBK캐피탈 로고(28px, 다크 페이지=logo-white/라이트=컬러 logo 자동 전환) + 우하단 "40TH ANNIVERSARY"
  문구(14px, 색도 전환). 엠블럼은 사용자 요청으로 제거. 전환은 html.kb-dark 클래스 — 키오스크 JS의
  IntersectionObserver가 darkP 목록(dots의 darkPages와 동일)으로 토글, **두 목록은 함께 갱신할 것**. .wrap만 zoom하므로
  100vh 페이지 골격·offsetTop(자동 투어 좌표)은 불변. moments 사진 마퀴(gal-rows)는 .wrap 밖이라 확대 제외
  (vh 정밀 배치 보호). .gal-note(호버·클릭 안내)와 차트 "표로 보기" details도 TV에서 무의미해 숨김.
  1920×1080에서 18페이지 전부 자손 단위 클리핑 0 검증(1.45까지 안전 확인, 1.4 채택 — TV 폰트 폴백 시
  줄바꿈 변동 여유분). 스토리 페이지가 가장 밀도 높음(1.45에서 패딩 박스 꽉 참). 배율 조정 시 넘침 재검증 필수.
  주의: .page{overflow:hidden}이라 scrollHeight로는 넘침 감지 불가 — 자손 getBoundingClientRect로 검증할 것
  (era-bg·vn-particles·page-wave·닫힌 details는 의도된 블리드/숨김이므로 제외).
- TV 검토 개선(2026-08-03, 사용자 선택 1~5·8번): ① 갤러리 kiosk 확대 — 카드 이미지 clamp(140px,16.2vh,175px)
  +캡션 15px/연도 13.5px(마퀴는 .wrap 밖이라 zoom 미적용 → 직접 상향), 커진 만큼 마퀴 1.45배 감속
  (animation-duration:calc(var(--dur)*1.45) — 인라인 --dur을 calc로 감속하는 패턴). 1920×1080에서 3줄 유지 검증.
  ② 축하 카드 cc-msg 16.5px·cc-meta 14px + 마퀴 1.4배 감속. ③ 클릭 전용 요소 숨김 — 헤더 **전체**(nav만 숨기다 사용자 요청으로 display:none 확대)·히어로
  CTA 2버튼·.scroll-hint. ④ D-day 승격 — 19px/굵은 숫자 23px(확대 포함 약 27/32px). ⑤ 저대비 텍스트 상향 —
  ev-d .6→.75, 다크 페이지 sec-desc .66→.8, duo-card span·pp-label→.78. ⑧ 페이지 전환 1100→1500ms(홈 복귀 2100ms).
  검토 시 기각: 오버스캔 safe margin(TV에서 "원본 크기(Just Scan)" 설정으로 해결), OLED 번인 대응(LCD 가정),
  상시 QR 코너 배지(화면 오염 — 축하 페이지 한정 유지).
- 구현: 커서 숨김, scroll-snap 해제(rAF 트윈과 충돌 방지 — 구형 TV 브라우저 대응), scroll-behavior:auto,
  rAF 이징 스크롤(백그라운드 탭에서는 rAF가 멈추므로 document.hidden이면 즉시 점프), Wake Lock(지원 시),
  6시간마다 자동 새로고침(메모리 예방). CSS는 저높이 압축 블록 **앞**에 위치(맨 끝 규칙 유지).
- KIOSK 상수는 메인 스크립트 최상단에서 정의 — 위치를 옮기면 자동재생 가드보다 늦게 평가되므로 금지.
- TV 쪽 설정: 절전/화면보호기 끄기 필요. 검증 완료(2026-08-03): 순회·정지·재개·음소거·폼 숨김·1920×1080 레이아웃.

## 사이트 구조 (풀페이지 스크롤 스냅, 18페이지)
1. 히어로 — 파도 캔버스(사가 재생 시 오디오 반응) + D-day 카운트다운(2026.11.1) + 40주년 엠블럼
2~5. 40년의 항해 — 스토리텔링 연혁 4챕터(첫 물결→폭풍을 지나→너른 바다로→혁신의 파도), 시대별 배경 일러스트.
   2026-07-22 사용자 요청으로 히어로 바로 다음으로 이동(스토리 먼저, 숫자는 나중).
   제1장 진입 시 "40년의 항해" 입장 인트로(.story-intro, 1.4초) — 역사관 입장 연출(2026-07-21).
   챕터별 기록 사진(.ch-photo 폴라로이드 카드, 2026-07-21): story1~4.jpg를 서사 옆에 배치, 시대별 CSS 필터
   (1장 세피아→4장 원색). 사진은 base64 내장이 아닌 **별도 파일 배포**(빌드 무관, 저장소에 함께 push).
   캡션은 사용자 지정 문구 — 임의 수정 금지. 원본 사진 아카이브(날짜_설명.jpg)는 .gitignore로 제외,
   story2·3.jpg는 사용자가 아카이브에서 직접 리네임한 파일.
6. 숫자 프롤로그(#numbers) — "40년이 남긴 숫자들" 입장 페이지. 여덟 숫자가 희미하게 부유(ni-bob 애니메이션):
   성장 4종(파랑)+사람의 힘 4종(금색 .g 클래스 — 두 장의 복선).
   히어로 "숫자로 보는 40년" 버튼이 여기로 연결 (2026-07-21 신설)
7~10. 성장 그래프 4페이지 — 총자산/당기순이익/자기자본/임직원.
   **숫자 인트로 연출**(2026-07-21): 페이지 진입마다 순수 숫자만(힌트·단위 전부 없음 — 궁금증 유도) 1.4초 오버레이(07-28 1.8→1.6→1.4초 단축)
   (.cp-intro/.intro-on)로 먼저 뜬 뒤 차트 드로잉(.chart-in)으로 전환. 사람의 힘 인트로(.pp-intro)·제1장 입장 인트로도 동일.
   2026-07-23 조정: 오버레이는 켜질 때 즉시 표시(transition:none — 뒤 내용 노출 방지), 꺼질 때만 페이드.
   감지 threshold 0.12 (페이지가 12%만 보여도 인트로 시작). prefers-reduced-motion이면 인트로 생략.
   (영업수익 그래프는 2026-07-20 사용자 요청으로 삭제. 성장 요약 배지는 임직원 페이지로 이동)
11. 숫자로 보는 40년 (요약) — 통계 타일 8종. "숫자 먼저, 요약은 나중" 구성으로 그래프 뒤에 배치.
    2026-07-24 확정 구성: 총자산 12.1조 / 당기순이익 2,456억 / 누적 순이익 1.8조(FY2006~25 합산 18,306억) /
    자기자본 "2조 759억 원" / 임직원 286명 / 1인당 총자산 422억(2.3배) / 총자산 CAGR 11.1% /
    금융자산 12조 달성 2026년(사용자 제공).
    삭제 이력: 기록 배지 전체·ROA·자기자본비율·"40년" 타일(07-23), 연속 흑자·신용등급 AA- 타일(07-24).
    자기자본 표기: 인트로 숫자만 "20,759", 이후(차트 헤더·라벨·눈금·툴팁·타일)는 조 단위 혼합 표기
    (eokjo 플래그 + fmtJo — 눈금은 0.5조/1조/1.5조/2조).
12~15. 사람의 힘 — 임직원 노력 지표를 지표당 1페이지(#power1~4, .power-page)로 구성:
    취급금액 69.7조 / 고객 2,709 / 시행문서 7,488건 / 14,610일. 각 페이지는 숫자 인트로(.pp-intro, 1.4초)
    → 서술형 문장 + 체감 비교 콜아웃(.pp-compare, 금색 배지 — 예: "하루 평균 약 270억 원")
    + 지표별 미니 비주얼(.pp-viz — 취급금액 연도별 스파크바 / 고객 기업·개인 분할카드+바 /
    시행문서 신전 5주 콜로네이드(.pp-temple — 지붕 보에 "실행을 떠받치다" 각인, 문구 기둥 2+빈 기둥 3,
    기둥 솟아오른 뒤 지붕 안착 연출) / 14,610일 40년 타임라인 + 돛단배(.tl-sail — 1986 출항→2026 정박, 흔들림))
    + 배경 워터마크 숫자(.pp-bg, 좌우 교차 — 성장 차트 페이지에도 .cp-bg로 동일 적용, 자기자본은 20,759 표기).
    2026-07-23 "밋밋함" 피드백으로 일러스트를 데이터 비주얼로 교체. 마지막 페이지에 "286명의 임직원" 클로징.
16. 함께한 순간들(#moments, .moments) — 2026-07-28 신설. 사용자 제공 20년치 활동 사진 173장(사진/ 폴더 원본)을
    3줄 폴라로이드 마퀴로 전량 표출. **연도 무관 랜덤 셔플(방문마다 다름)** — "어느 해였든 모두가 한마음" 콘셉트
    (사용자가 연대순 대신 명시적으로 선택). 제목 "함께하는 선원들" + 부제 "어느 해의 파도였든, 우리는 늘
    한마음으로 같은 바다에 있습니다." (2026-07-28 사용자 확정 문구 — 임의 수정 금지).
    구현: `build_gallery.py`가 사진/→photos/g###.jpg 썸네일(높이 340px, q77, 총 4.7MB) 생성 +
    src.html의 `const GALLERY=[...];` 한 줄 자동 교체(파일명/캡션/연월/나눔플래그/썸네일 w·h). 사진은 base64 아닌 **별도 파일 배포**.
    라이트박스 확대본 f###.jpg(높이 최대 800px q80, 업스케일 없음)도 함께 생성 — photos/ 총 346파일 13.4MB(확대본은 클릭 시에만 로드).
    **실명 제거 필수**: 원본 파일명의 "(김재원 외)" 류 괄호는 캡션에서 제거, 배포 파일명도 g###.jpg 중립 번호
    (공개 사이트 — URL·캡션 모두 이름 노출 금지). 사회공헌·봉사·헌혈·연탄·나눔 키워드 → 금색 "나눔" 태그(15장).
    카드는 높이만 vh-clamp(14.33vh, 100~140px) 고정, **폭은 사진 원본 비율 그대로**(width:auto + w/h 속성) —
    07-28 얼굴 잘림 피드백으로 3:2 강제 크롭 폐지(세로·4:3 사진 머리 잘림 방지, 잘림 0건 검증). 초광각 가드로
    max-width:min(320px,48vh)만 유지. 캡션 폭은 width:0+min-width:100% 트릭으로 이미지 폭에 맞춤.
    모든 화면 높이에서 3줄이 딱 맞음(375/720/900 검증).
    줄별 반대 방향 흐름(cheerflow 키프레임 재사용), 호버 시 일시정지+틸트 해제 확대, lazy load,
    prefers-reduced-motion 시 정지. 원본 사진/ 폴더는 .gitignore 제외.
    라이트박스(07-28): 카드 클릭 → 어두운 블러 배경(rgba(4,10,26,.78)+blur 7px) 위 큰 폴라로이드(.gal-lb,
    최대 68vh)로 f###.jpg 표시, 배경 클릭·×·ESC로 닫힘. 카드/배경 커서 zoom-in/zoom-out.
    긴 캡션 처리(2026-07-28): ① build_gallery.py가 캡션 자체 축약 — "해외박람회(X)"는 X만("CES 2018" 형식
    정규화, 로스엔젤레스→LA 등), 연수 괄호는 차수 제거·쉼표→가운뎃점. ② 그래도 잘리는 캡션(…)은 호버 시
    옆으로 슬라이드되며 전체 표시 — .tx(inline-block) + translateX(min(0px,calc(100cqw - 100%))),
    .t는 container-type:inline-size + **flex:1 필수**(크기 봉쇄라 flex:1 없으면 너비 0으로 붕괴하는 버그 있었음).
    미지원 브라우저는 … 유지로 우아한 저하.
17. 임직원 축하 한마디 — **Firebase Firestore 실시간 연동** (아래 상세)
18. 비전 + 푸터
- 사업영역 페이지(기업금융/투자금융/할부리스/글로벌 4카드)는 2026-07-22 사용자 요청으로 삭제됨
- 오른쪽 도트 내비게이션, 헤더에 사가 재생 버튼

## Firebase (임직원 축하 한마디) — 구축 완료, 동작 검증됨
- 프로젝트: `ibk-40th` (Spark 무료), Firestore 서울(asia-northeast3), 프로덕션 모드
- 콘솔: https://console.firebase.google.com/project/ibk-40th/firestore
- 컬렉션 `cheers`, 문서: {name: string(≤20), msg: string(≤160), ts: serverTimestamp}
- 게시된 보안 규칙: 읽기 전체 허용 / create만 허용(필드·길이 검증) / update·delete 차단
- 클라이언트: src.html 하단 `<script type="module">` — gstatic CDN v10.12.2, onSnapshot 실시간 구독(최신 100개),
  addDoc 제출, localStorage 기반 1분당 1회 제한, textContent 렌더링(XSS 안전)
- 카드 표기(2026-07-24): 날짜는 표시하지 않음(ts는 저장·정렬용으로 유지). 이름 미입력 시 대체 닉네임 —
  수식어 50 × 바다 명사 50 = 2,500가지 조합(쉽고 귀여운 톤 — 바다 생물 포함). **제출 시 랜덤으로 뽑아 name 필드에 저장**(영구 고정).
  nickFor(문서 ID 해시)는 이름 없는 옛 문서용 폴백. 닉네임 최대 11자라 name ≤20 규칙 통과
- firebaseConfig는 src.html에 내장돼 있음 (apiKey는 공개용 값이라 커밋해도 안전)
- 메시지 삭제(운영): Firebase 콘솔 → Firestore → cheers 컬렉션에서 문서 삭제 → 전 방문자 실시간 반영
- 테스트 메시지는 2026-07-23 사용자가 콘솔에서 전부 삭제 완료 — DB 깨끗한 상태

## 주요 기능 구현 메모 (버그 이력 포함 — 재발 방지)
- **사가 자동재생**: 로드 시 play() 시도, 차단되면 첫 사용자 이벤트에서 재시도.
  ⚠️ 리스너는 재생이 "성공할 때까지" 유지해야 함. 휠/스크롤은 브라우저가 제스처로 인정하지 않아
  play()가 거부되는데, 과거에 첫 이벤트에서 리스너를 제거해버려 음악이 영영 시작 안 되는 버그가 있었음.
- **오디오 비주얼라이저(Web Audio)**: AudioContext는 반드시 진짜 제스처(pointerdown/keydown/touchstart)
  문맥에서만 생성. suspended 상태에서 createMediaElementSource를 연결하면 소리가 사라지는 무음 버그 있음.
- **가사 자막(CUES 배열)**: 사용자가 LRC 메이커(lrc-maker.github.io)로 직접 찍은 실측 타임코드(0.1초 단위).
  임의 수정 금지. 첫 가사 13.71s (2026-07-20 사용자 재실측 LRC로 갱신). 1:15~1:27 사이 12초 간주 구간에 "♪ ── 간주 ──" 표시 로직 있음.
  가사 박스는 20px(모바일 15px) — 사용자가 1.4배 확대 요청했던 결과값.
- **차트(renderChart)**: 선그래프는 y축을 데이터 범위로 확대(0 시작 아님, 각주로 명시), 막대는 항상 0 기준
  (길이 왜곡 방지). 2026-07-21 극적 연출 강화: 선그래프 축 경계를 눈금 배수로 내림하지 않고 데이터에
  밀착(하단 5%·상단 7% 패딩만)시켜 선이 플롯의 ~90%를 쓰게 함. 눈금은 범위 안의 배수 위치에만 표시.
  차트 높이 H=360 (기존 330). 2026-07-23 강조 연출 추가: 선그래프 이정표 온차트 주석(marks 배열 — 총자산 '10조 돌파',
  자기자본 증자 2건), 순이익 2008 금융위기 음영 밴드(bands 배열), 끝점 펄스 링(.cv-pulse), 면 그라데이션(defs area-*),
  라인 글로우, 차트 전환 시 헤더 값 카운트업(cp-value data-target). 사람의 힘 비주얼도 인트로 해제 시 등장 연출
  (스파크바 자라남·분할바 채워짐·타임라인 순차 점등, intro-on 클래스 토글로 재진입마다 재생). 로그 스케일은 구현했다가 사용자 요청으로 제거함. 드로잉 애니메이션은 .chart-in 클래스
  (IntersectionObserver, 페이지 재진입 시 재실행).
- **풀페이지 스냅**: html{scroll-snap-type:y mandatory}, 모바일/저높이 화면에서는 proximity로 완화.
- **저높이 압축 블록**(2026-07-23): `@media(max-height:820px)` 규칙 세트가 **스타일시트 맨 끝**에 있음 —
  같은 특이성의 기본 규칙보다 뒤에 와야 이겨서 위치를 옮기면 안 됨. 큰 화면(높이>820px)에는 미적용.
  중간 폭 헤더는 nowrap + @media(max-width:1080px) 간격 축소 + 1000px 이하 "40TH ANNIVERSARY" 숨김으로 처리.
- **폰트**: Pretendard(jsdelivr CDN), 실패 시 시스템 폰트 폴백.

## 데이터 출처 (전부 공시·공식 자료로 검증됨 — 임의 수정 금지)
- **2026-07-20 전면 재구축**: 그래프 4종(총자산·순이익·자기자본·임직원) 전체를 DART 사업보고서
  FY2006~FY2025 20개년에서 직접 추출한 값으로 통일. **전부 별도 재무제표 기준** (연결 아님).
  2006~2010은 K-GAAP, 2011부터 K-IFRS — 경계는 각 그래프 각주에 명시.
- 회계기준 결정 이력: 사이트의 주요경영지표·재무현황·통계 타일(ROA·자기자본비율 포함)이 별도 기준이라
  전체를 별도로 통일함. 사용자가 한때 제공한 원 단위 수치는 연결 기준이었음이 사업보고서 대조로 판명되어 폐기.
  연결 수치가 필요하면 사업보고서 요약재무정보에 병기돼 있음. (참고: 사용자 제공 FY2020 연결 순이익
  151,133,466,203원은 오타 — 보고서 원문은 153,133,466,203원)
- 임직원 수 = **직원 + 상근 임원** (2026-07-21 확정): 사업보고서 「임원 및 직원 현황」 직원 수에
  같은 보고서 임원 명단에서 계수한 상근 임원(등기 상근 + 미등기, 사외이사·비상근 제외, 연말 기준)을 합산.
  2006년 87명 → 2025년 286명 (3.3배). 연도별 임원 수: 4/6/6/6/6/6/4/4/4/5/8/7/8/10/11/12/10/10/10/11.
  유의: FY2012~2014는 미등기 명단 자체가 없어 0으로 계수(부존재 단정 아님), FY2022는 연말 명단(8명) 기준
  (보수현황 표의 9명은 연중 퇴임자 포함으로 판단). 이전 FISIS 시계열(2010년 104명이 서무직 제외값)은 폐기됨.
- ROA·자기자본비율: IBK캐피탈 주요경영지표 (ibkc.co.kr, 별도 기준) — 2026-07-23 타일에서 제거됨 (수치는 참고용으로 보존)
- 연혁: IBK캐피탈 공식 주요연혁 페이지 + 사업보고서 회사의 연혁 (증자·사명변경·대표이사 취임 연월 확정)
- 원본 PDF 20부는 로컬 폴더에 있으며 .gitignore(*.pdf)로 저장소에서 제외됨

## 개발 히스토리 요약 (Cowork 세션에서 진행된 순서)
1. Canva 커넥터 연동 테스트 → 40주년 포스터 시안 4종 생성 (참고용)
2. 홈페이지 초안: 히어로/통계/연혁/사업영역/비전 (단일 HTML)
3. 성장 그래프 추가 → 2010년대 데이터 발굴(기업은행 감사보고서·FISIS)로 15개년 시계열 확장
4. 사가 MP3 내장 + 재생 버튼 → 자동재생 + 버튼 문구 "40주년 헌정곡 - 혁신의 파도"
5. GitHub Pages 배포 (저장소 gongmo-ibkc/ibk-capital-40th, 웹 업로드 방식)
6. 임직원 수 그래프 추가 (FISIS를 브라우저로 직접 조회해 수집)
7. 파도 콘셉트 리디자인 + 4챕터 스토리텔링 연혁
8. 풀페이지 스크롤 전환, 통계 8타일 확장, 그래프별 1페이지
9. y축 로그 토글 추가 → 제거하고 데이터 범위 확대 방식으로 변경
10. 고도화 7종: 차트 드로잉 애니메이션/엠블럼/OG 카드/시대별 일러스트/가사 자막/D-day/모바일 점검
11. 가사 싱크: 에너지 분석 → 사용자 실측 LRC로 최종 확정
12. 축하 한마디: Google Sheets 연동 → Firebase Firestore 실시간으로 전환 (콘솔 설정 포함 완료)

## "사람의 힘" 섹션 데이터 (전부 사용자 제공 내부 데이터 — 2026-07-21 반영 완료)
- 취급금액 (억 원, 연도별): 2016 54,666 / 2017 54,533 / 2018 56,139 / 2019 60,594 /
  2020 68,242 / 2021 84,321 / 2022 68,069 / 2023 55,634 / 2024 74,067 / 2025 82,167 / 2026(1~6월) 38,176
  → 누적 696,608억 = 약 69.7조 (2016~2026.6). 40주년 시점에 하반기 실적 반영해 갱신 여지 있음.
- 고객 수: 기업 1,701 / 개인 1,008 = 2,709 (2026.6월 말 기준)
- 시행문서: 7,488건 (2026.6월 말 기준). **의미 주의**: 약정서·계약서가 아니라 업무처리 규정·절차를 안내하는
  내부 문서임 — "금융 실행을 뒷받침하는 윤활유·연료" 콘셉트로 서술 (2026-07-23 사용자 교정)
- 취급금액 "하루 평균 약 270억": **영업일 기준** — 2016.1.1~2026.6.30 평일 2,738일에서 평일에 걸린
  한국 공휴일(대체공휴일 포함) 148일을 뺀 영업일 2,590일로 계산(696,608억÷2,590≈269억). 2026-07-29 검증,
  pp-foot에 "영업일 약 2,590일 기준" 명기. 달력일 기준이면 약 182억이므로 "매일"로 바꾸면 숫자도 바꿔야 함.
- 미사용 후보(추후 활용 가능): 최근 20년 누적 순이익 약 1조 8,306억(FY2006~2025 별도 합산),
  1인당 총자산 195억(2006)→439억(2025)

## 백로그 (사용자가 계획 중인 것)
- CEO 인사말, 사진 자료(창립 초기 사진 등), 브랜드 컬러 반영 — 자료 수급 대기 (공식 로고는 2026-07-20 헤더·푸터 반영 완료)
- 40주년 행사 정보, 참여형 콘텐츠 확장
- "전문 페이지 수준" 디자인·효과 고도화 — 콘텐츠를 먼저 채운 뒤 진행하기로 합의됨
- 후보 아이디어(남은 것): 커서 파문 효과, 페이지 전환 파도 와이프, 스탯 타일 호버 스파크라인,
  사가 가사-히어로 연동 연출, 인트로 숫자 카운트업, 축하 카드 호버 일시정지
- 기각된 실험(2026-07-25, 사용자 확인 후 롤백): 히어로 40 물결 채우기 마스크, 헤더 항해 진행선(돛단배 마커),
  비전 페이지 엠블럼 메달 — 재제안 금지. 채택된 것: 챕터 배경 표류(era-drift), 비전 새벽 그라데이션+금색 입자
