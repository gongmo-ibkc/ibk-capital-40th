# 참고자료 (DART 사업보고서 등) — 저장소에 올리지 않음
*.pdf

# 원본 사진 아카이브 (날짜_설명 형식) — 사이트에 쓰는 최적화본(story*.jpg 등)만 커밋
[0-9]*_*.jpg
[0-9]*_*.png
story[0-9].png

# 원본 사진 폴더 (배포는 photos/ 썸네일만)
사진/

# 공모전 기획서 (내부 제출용 — 공개 저장소 제외)
*.pptx
